# Adaptive Media v0.1.0

Standalone Windows media playback setup, published on a dedicated branch so the main LocalAI project is untouched.

Download this branch as a ZIP, extract it, and run `AdaptiveMediaSetup.cmd`.

This branch is intentionally isolated from the LocalAI source tree.
