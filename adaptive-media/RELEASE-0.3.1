trigger Adaptive Media 0.3.1 Windows build, isolated smoke validation, and GitHub release
fix: mpv video-sync-max-factor must stay within supported 1..10 range
retry: clean in-launcher explanations and pin v0.3.1 tag to the validated build commit
