# Adaptive Media 0.4.0-rc1 prerelease coordinates.
#
# These coordinates name the exact installer that was validated. Publication
# uploads that artifact and refuses to proceed if its SHA-256 does not match the
# digest below. Nothing is rebuilt between validation and publication.
#
# Published as a prerelease. v0.3.2 remains the latest release and is not
# modified. 0.4.0-rc1 installs side by side under its own AppId and its own
# settings directory.
#
# Validated on the RTX 4080 Laptop GPU / Intel UHD hybrid machine with
# mpv v0.41.0-1011-g182fa6ca4:
#
#   Plan matrix               226 assertions PASS, including real probes of
#                             H.264, HEVC 10-bit, AV1, VP9 10-bit, MPEG-4 and
#                             non-ASCII filenames
#   Settings                  20 assertions PASS, plus harness failure reporting
#   Packaging / reconstruction PASS on Windows PowerShell 5.1 and PowerShell 7
#   Real-media RTX regression PASS on both runtimes, ASCII and non-ASCII paths
#   Installer smoke           install -> self-test -> integration-test ->
#                             upgrade -> uninstall PASS, settings sentinel
#                             byte-identical throughout
#   Hardware end to end       RTX, NVIDIA reference, Compatibility and Reference
#                             lanes PASS; real mpv argv matched the canonical
#                             plan fingerprint in every run
#   RTX adaptation            1.067x windowed -> 1.333x fullscreen -> off when
#                             downscaled -> 1.333x restored
#   Frame drops               0 output, 0 delayed
#
# Not verified: HDR switching on a real HDR HDMI display, HDMI bitstream on a
# real endpoint, live provisioning on a clean machine, non-NVIDIA and multi-GPU
# topologies, and URL playback with a current yt-dlp.

commit = 37f33470c908ec4c9a3e1cd47791505cd7281165
branch = astra/adaptive-media-upgrade-20260905-112301
installer = AdaptiveMediaSetup-0.4.0-rc1-x64.exe
sha256 = 0A275527A0694FBF65B9568F53956C3035FE6C0CF0D2EC4B47AB32FABFC73634
bytes = 43903692
built_utc = 2026-09-06T03:09:18Z
prerelease = true
